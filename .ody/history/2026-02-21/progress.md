# Progress Log

Generated: 2026-02-21T03:51:36.395Z

---

2026-01-29: Added --provider/--model args to init to persist optional config fields; marked PRD complete.
2026-02-03: Added basic Bun tests for Test task (Test 1/2) and marked PRD entry complete.
2026-02-03: Added `ody plan` command stub with console message; marked PRD entry complete.
2026-02-03: Implemented interactive `ody plan` prompts using @clack/prompts and console output; marked PRD entry complete.
2026-02-05: Fixed proc.kill(0) zombie process bug — replaced proc.kill(0) with proc.kill() + await proc.exited in run.ts loop mode, removed unnecessary proc.kill(exitCode) in run.ts once mode, replaced proc.kill(0) with proc.kill() in plan.ts; marked PRD entry complete.
2026-02-05: Fixed stderr pipe deadlock — changed stdio from ['ignore','pipe','pipe'] to ['ignore','pipe','inherit'] in run.ts and plan.ts so stderr flows directly to the terminal instead of filling an unread pipe buffer; marked PRD entry complete.
2026-02-05: Fixed maxIterations: 0 bug — updated for-loop in run.ts to treat 0 as infinite (`maxIterations === 0 || i < maxIterations`), added `.int().nonnegative()` constraints to zod schema in config.ts; marked PRD entry complete.
2026-02-05: Fixed silent swallowing of non-Error exceptions — added else branches to all 4 catch blocks (config.ts, init.ts, run.ts x2) so non-Error thrown values (strings, objects, ZodError) are logged via String(err) and trigger process.exit(1) instead of being silently ignored; marked PRD entry complete.
2026-02-05: Made Claude --dangerously-skip-permissions flag configurable — added optional `skipPermissions` boolean (default true) to zod config schema, added confirm prompt during `ody init` when backend is claude, updated claude.ts to conditionally include the flag based on config; marked PRD entry complete.
2026-02-05: Added plan iteration loop — wrapped the entire plan flow in a while loop with a "Would you like to add another plan?" confirm prompt after each iteration; user can create multiple plans in one session or decline to exit gracefully; also handles cancel (Ctrl+C) at the confirm prompt; marked PRD entry complete.
2026-02-05: Added verbose stderr logging to run command — when --verbose is passed, stderr is piped instead of inherited and drained concurrently via an async IIFE that logs each chunk through logger.warn, preventing buffer deadlock while keeping stderr as 'inherit' when verbose is off; marked PRD entry complete.
2026-02-05: Added verbose stderr logging to plan command — mirrored the run command pattern: when --verbose is passed, stderr stdio changes from 'inherit' to 'pipe' and is drained concurrently via an async IIFE that logs each chunk through logger.warn; stderr remains 'inherit' when verbose is off; stdout completion marker detection is unaffected; marked PRD entry complete.
2026-02-10: Completed task 'Add Unit Tests for Util Modules' - Created 3 test files (constants.test.ts, stream.test.ts, inputPrompt.test.ts) with 21 passing tests covering all util modules. All tests pass via 'bun test packages/cli/src/util'.
2026-02-10: Completed task - Configurable Tasks Directory. Added tasksDir config option with default '.ody/tasks', updated LOOP_PROMPT and PLAN_PROMPT to use dynamic path substitution.
2026-02-10: Added label filter flag (-l/--label) to run command with task filtering and dry-run integration
2026-02-10: Removed confirmation prompt from plan command — removed the box() display and confirm() prompt that asked "Proceed with these plan details?" after entering a task description. Plan generation now starts immediately after the user submits their description, reducing friction. Cleaned up unused imports (box, cancel).
2026-02-10: Added `ody config` command — created packages/cli/src/cmd/config.ts that displays current configuration from .ody/ody.json using Config.all(), registered it as a subcommand in index.ts. Handles missing config gracefully with a helpful message.
2026-02-10: Added --iterations/-i flag to run command — allows overriding maxIterations config at invocation time. Validates input as non-negative integer, supports 0 for unlimited. No effect in --once mode. All validation commands pass.
2026-02-10: Added PTY-based completion detection to --once mode — replaced inherited stdio with Bun.Terminal PTY API in run.ts so the <woof>COMPLETE</woof> marker is detected and the process is killed automatically, while preserving full interactive TTY behavior for the child process.
2026-02-10: Added `plan list` subcommand — restructured plan.ts to support subcommands using citty's subCommands pattern. Existing plan creation logic preserved as default behavior (`ody plan`) and also available as `ody plan create`. New `list` subcommand reads .code-task.md files from the tasks directory, parses YAML frontmatter, and displays pending tasks with titles and filenames. Handles missing directory and no-pending-tasks cases gracefully.
2026-02-10: Added OS notifications on completion — created packages/cli/src/lib/notify.ts with cross-platform sendNotification() using osascript (macOS) and notify-send (Linux). Added `notify` config field (false | true | 'all' | 'individual') to Zod schema, --no-notify flag to run command, and notification prompt to init wizard. Notifications fire on loop completion ('all'), per iteration ('individual'), or in single-shot mode.
2026-02-10: Added global config file support — refactored Config.load() in packages/cli/src/lib/config.ts to detect and load a global config from ~/.ody/ody.json (with XDG fallback at ~/.config/ody/ody.json). Global config is merged with local project config, with local values taking precedence. Uses Bun.env.HOME with os.homedir() fallback for cross-platform home directory detection. Fully backward-compatible — existing setups without global config continue to work unchanged.
2026-02-10: Added `plan edit` subcommand — added an `edit` subcommand to the plan command that discovers .code-task.md files in the tasks directory, presents a select prompt for the user to choose a file, reads its content, constructs an edit prompt, and spawns the backend harness in build mode to edit the file. Supports --dry-run and --verbose flags. Registered as `ody plan edit` alongside existing create and list subcommands.
2026-02-10: Added task file positional argument to run command — `ody run <file>` now accepts an optional positional argument pointing to a specific .code-task.md file. When provided, the agent targets only that task (using a dedicated SINGLE_TASK_PROMPT), defaults to 1 iteration, and exits on completion. Validates file existence and extension; mutually exclusive with --label. Works in both loop and --once modes. Updated buildRunPrompt in runPrompt.ts to support the taskFile option.
2026-02-10: Added `plan compact` subcommand — archives completed tasks into a single markdown file at .ody/history/archive-{date}.md. Scans .code-task.md files, filters for status: completed, extracts titles and condensed descriptions, sorts by completion date, writes the archive, and deletes original completed task files. Handles edge cases (no tasks directory, no completed tasks, empty directory).
2026-02-12: Added configurable `agent` field to ody config — added optional `agent` string (default 'build') to the zod config schema, updated Backend.buildCommand and buildOnceCommand to read from config instead of hardcoding 'build', and added --agent/-a flag and interactive prompt to the init command for persisting the value.
2026-02-12: Added unit tests for lib directory — created packages/cli/src/lib/__tests__/ with four test files: sequencer.test.ts (pure-function incrementing counter tests), config.test.ts (Config.parse validation with valid/invalid inputs, access guard tests for all() and get()), notify.test.ts (platform-branching behavior, error swallowing, and contract tests for sendNotification), and logger.test.ts (level check, fatal reporter triggers process.exit). 35 new tests, all passing. Full suite: 56 tests across 7 files.
2026-02-13: Removed --once flag and logic from run command — deleted --once and --dry-run arg definitions and the entire PTY-based single-execution code path from run.ts, removed buildOnceCommand abstract method from Harness base class, removed buildOnceCommand from Backend facade, and removed buildOnceCommand implementations from claude.ts, opencode.ts, and codex.ts. Updated plan edit subcommand to use buildCommand with piped stdio instead of buildOnceCommand. Updated README.md to remove --once and --dry-run references. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Completed task 'Write Test File to ./test/1.txt' — created test/1.txt with content "TEST 1", verified file exists with correct content. All validation commands pass.
2026-02-15: Refactored `plan` command to batch description collection before agent prompting — split the single while loop into a collection phase (gathers all descriptions upfront via text+confirm) and a processing phase (iterates descriptions sequentially, invoking the agent for each). Added batch progress indicators in spinner messages, try/catch per plan to prevent single failures from aborting the batch, graceful cancellation during collection, and a summary outro message. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Promoted `compact` from `ody task compact` to root-level `ody compact` — moved compact.ts from cmd/task/ to cmd/, updated relative import paths (../../ → ../), registered as root-level subcommand in index.ts, removed from task subcommands in cmd/task/index.ts, deleted old file. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Extended `compact` command to archive progress.txt alongside completed tasks — changed history output from `.ody/history/archive-{DATE}.md` to date-stamped subdirectories (`.ody/history/{DATE}/tasks.md` and `.ody/history/{DATE}/progress.md`). Reads and archives progress.txt content, clears the original after archiving. Updated early-exit logic so the command proceeds if either completed tasks or progress entries exist. Handles missing tasks dir and missing/empty progress.txt gracefully. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Added package metadata to packages/cli/package.json — bumped version from 0.0.1 to 0.1.0, added description ("Agentic orchestrator CLI"), license (MIT), author (8bittitan), and repository object. All existing fields preserved unchanged. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Initialized Changesets for version management — installed @changesets/cli as root dev dependency, scaffolded .changeset/ directory with config.json and README.md, configured with access: restricted, commit: false, baseBranch: main. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created Prepare Release GitHub Actions workflow — added .github/workflows/prepare-release.yml with workflow_dispatch trigger (bump type choice + optional summary), Bun setup via .tool-versions, inline changeset creation, changeset version consumption, and PR creation via peter-evans/create-pull-request with release commit message format. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created Release (Tag) GitHub Actions workflow — added .github/workflows/release.yml that triggers on push to main when packages/cli/package.json changes. Detects release commits by checking for 'release: v' prefix, reads version from package.json via jq, creates a lightweight git tag (v<version>), and pushes to origin. Includes idempotency check to skip existing tags. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created Binaries GitHub Actions workflow — added .github/workflows/binaries.yml that triggers on v* tag pushes. Builds cross-platform Bun binaries in parallel using a matrix strategy (darwin-arm64, darwin-x64, linux-x64, linux-arm64), uploads each as an artifact, then creates a GitHub Release with all binaries attached via softprops/action-gh-release. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created user-facing install script — added install.sh at repo root. POSIX sh script with set -eu, detects OS (darwin/linux) and architecture (x64/arm64), fetches latest release tag from GitHub API, downloads the correct binary to $ODY_INSTALL_DIR (default: ~/.local/bin), makes it executable, and warns if install dir is not in PATH. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Integrated config schema generation into release pipeline — added a `schema` job to .github/workflows/binaries.yml that runs `bun run gen:schema` from packages/cli, uploads configuration_schema.json as an artifact, and updated the `release` job's needs to [build, schema] so the schema is included in GitHub Release assets alongside platform binaries. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Added Installation section to README.md — documented three install methods: install script (curl one-liner with ODY_INSTALL_DIR override), manual binary download from GitHub Releases (listing all four platform targets), and build from source. Placed between Requirements and Quick start sections. Updated Quick start to reference the Installation section and show installed-binary usage first. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Added file argument to plan command for batch task generation — added optional positional `planFile` arg to `ody plan`, new `buildBatchPlanPrompt` function in planPrompt.ts that instructs the agent to read a planning document and create multiple .code-task.md files, and branching logic in plan.ts that validates file existence, invokes agent once with batch prompt, and supports --dry-run and --verbose. Interactive mode remains unchanged when no file is provided. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created packages/docs workspace package — bootstrapped packages/docs with package.json (@ody/docs, private, type: module), standard Next.js scripts (dev, build, start), runtime deps (next ^15.3.4, react ^19.1.0, fumadocs-core ^15.8.5, fumadocs-ui ^15.8.5, fumadocs-mdx ^14.2.7), dev deps (@types/mdx, @types/react, @types/react-dom, typescript, tailwindcss, postcss, autoprefixer, vite), and .gitignore (.next/, .source/, node_modules/). Ran bun install successfully, workspace recognized by Bun. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Added Next.js and Fumadocs configuration files to packages/docs — created next.config.mjs (wrapped with createMDX from fumadocs-mdx/next), tsconfig.json (extends root, adds fumadocs-mdx:collections/* path alias to .source/*), source.config.ts (defineDocs collection pointing to content/docs), postcss.config.mjs (@tailwindcss/postcss plugin for Tailwind v4), and tailwind.config.ts (content paths for app, content, lib, and fumadocs-ui). Installed @tailwindcss/postcss as dev dependency. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created app root layout, global styles, and home page for docs site — added packages/docs/app/layout.tsx (root layout with RootProvider from fumadocs-ui, metadata export), packages/docs/app/global.css (imports fumadocs-ui styles and Tailwind v4), and packages/docs/app/page.tsx (redirects / to /docs). All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created docs layout, catch-all page, and source loader — added packages/docs/lib/source.ts (Fumadocs source loader wired to MDX collections with /docs baseUrl), packages/docs/app/docs/layout.tsx (DocsLayout with sidebar navigation from source page tree), and packages/docs/app/docs/[[...slug]]/page.tsx (catch-all route with MDX rendering via DocsPage/DocsBody, generateStaticParams, generateMetadata, and notFound handling). All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created core documentation content pages — added packages/docs/content/docs/meta.json (sidebar ordering with Introduction/Installation/Configuration sections and Commands reference), packages/docs/content/docs/index.mdx (Introduction page covering key concepts: backends, task plans, agent loop, prompt building), packages/docs/content/docs/installation.mdx (install script, release binary download, build from source), and packages/docs/content/docs/configuration.mdx (all config keys with types/defaults/descriptions, prompt template, config resolution order, programmatic API). All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created commands documentation pages — added packages/docs/content/docs/commands/ directory with meta.json (sidebar ordering: index, init, run, config, compact, plan), index.mdx (commands overview with reference table), init.mdx (all flags, interactive setup flow, examples), run.mdx (positional taskFile arg, --iterations/--label/--verbose/--no-notify flags, loop/single-task/label modes), config.mdx (behavior, example output), and compact.mdx (task/progress archival, output structure). All documentation matches source implementations. All validation commands pass (lint, fmt, typecheck).
2026-02-15: Created plan subcommands documentation — added packages/docs/content/docs/commands/plan/ directory with meta.json (sidebar ordering: index, new, list, edit, compact), index.mdx (plan system overview with task file format reference), new.mdx (ody plan interactive and batch modes with --dry-run/--verbose flags), list.mdx (ody task list behavior and output), edit.mdx (ody task edit interactive selection with --dry-run/--verbose flags), and compact.mdx (ody compact archival behavior). All flag documentation verified against source implementations in plan.ts, task/list.ts, task/edit.ts, and compact.ts. All validation commands pass (lint, fmt, typecheck).
2026-02-17: Added `ody update` self-update command — created packages/cli/src/cmd/update.ts with GitHub Releases API integration to check for and install newer CLI versions. Supports --check/-c flag for check-only mode, semver comparison, platform/arch artifact resolution (darwin/linux, arm64/x64), user confirmation prompt, binary download and in-place replacement with chmod, and comprehensive error handling for network failures, unsupported platforms, and permission errors. Registered as lazy-loaded subcommand in index.ts. All validation commands pass (lint, fmt, typecheck).
2026-02-17: Added root-level docs convenience scripts — added `docs:dev` and `docs:build` scripts to root package.json that run `bun run --filter '@ody/docs' dev` and `bun run --filter '@ody/docs' build` respectively, alongside the existing `dev:docs` script. All validation commands pass (lint, fmt, typecheck).
2026-02-17: Added documentation page for the `ody update` command — created packages/docs/content/docs/commands/update.mdx with Synopsis, Flags (--check/-c), Behavior (default update flow, check-only mode, already-up-to-date, error handling), Examples, and Related sections. Added "update" to meta.json pages array and added update row to commands index.mdx reference table. All validation commands pass (lint, fmt, typecheck).
2026-02-18: Added `ody auth jira` command — created packages/cli/src/lib/auth.ts (Auth namespace with setJira/resolveAuthPath for managing credentials in ~/.local/share/ody/auth.json), packages/cli/src/cmd/auth/index.ts (extensible parent command), and packages/cli/src/cmd/auth/jira.ts (interactive credential flow with --profile flag, @clack/prompts for email/password input, cancel handling, and success messaging). Registered auth as root subcommand in index.ts, added 'auth' to SKIPPABLE_COMMANDS so it works without project config. All validation commands pass (lint, fmt, typecheck).
2026-02-18: Enhanced Auth store with XDG base directory support — rewrote packages/cli/src/lib/auth.ts to use xdg-basedir package for resolving $XDG_DATA_HOME (falling back to ~/.local/share), added Zod schema validation for auth.json parsing, added chmod 0o600 for secure file permissions, exported public load()/save()/getJira()/resolveAuthPath() methods and JiraCredentials/AuthStore types. Installed xdg-basedir as dependency. All validation commands pass (lint, fmt, typecheck).
2026-02-18: Added Jira configuration to config schema — extended configSchema with optional `jira` object containing `baseUrl` (z.string().url()) and `profile` (optional string) fields, added matching annotated field to Config.Schema with .describe() calls for JSON schema generation. OdyConfig type automatically includes the new field via z.infer. Existing configs without jira key parse without errors. All validation commands pass (lint, fmt, typecheck).
2026-02-18: Added Jira REST API client — created packages/cli/src/lib/jira.ts with Jira namespace exporting parseInput (handles full URLs and ticket keys, supports /browse/ and /jira/browse/ URL patterns), fetchTicket (GET /rest/api/2/issue/{key} with optional Basic auth, HTTP error handling for 401/403/404), and formatAsDescription (structured multi-line text output). Defined JiraTicket and ParsedInput types. Uses native fetch() and btoa(). All validation commands pass (lint, fmt, typecheck).
2026-02-18: Added Jira import prompt builder — created packages/cli/src/builders/importPrompt.ts exporting buildImportPrompt({ ticketData }) function. Template reuses the same OVERVIEW, RULES, and FILE FORMAT sections as planPrompt.ts for consistent .code-task.md output, with a Jira-specific IMPORTED JIRA TICKET section that instructs the agent to map ticket summary to task name, description/comments to Background and Technical Requirements, infer Implementation Approach and Acceptance Criteria, and include the Jira ticket key in Labels metadata. Replaces {TICKET_DATA}, {CURRENT_DATE}, and {TASKS_DIR} placeholders. All validation commands pass (lint, fmt, typecheck).
2026-02-18: Added `ody task import --jira` command — created packages/cli/src/cmd/task/import.ts with full Jira ticket import workflow: parses ticket key or full URL via Jira.parseInput, resolves credential profile from config (defaults to "default"), fetches ticket with spinner feedback, formats ticket data and builds import prompt via buildImportPrompt, supports --dry-run (prints prompt without spawning agent) and --verbose (streams agent output). Spawns backend agent with piped stdio, detects <woof>COMPLETE</woof> completion marker to kill the process. Registered as lazy-loaded subcommand in task/index.ts. All validation commands pass (lint, fmt, typecheck).
2026-02-18: Added `ody auth list` command — created packages/cli/src/cmd/auth/list.ts with listAuthCmd that displays all configured authentication credentials. Loads global auth store via Auth.load(), optionally merges local project config for Jira baseUrl and active profile. Displays each Jira profile with email, masked API token (******+last 6 chars), and active profile indicator. Handles no-credentials case with helpful message directing to `ody auth jira`. Registered as lazy-loaded subcommand in auth/index.ts. All validation commands pass (lint, fmt, typecheck).
2026-02-18: Added GitHub auth provider with profile support — extended authStoreSchema in auth.ts with `github` key (z.record of githubCredentialsSchema with `token` field), exported GitHubCredentials type, added Auth.getGitHub/setGitHub methods mirroring the Jira pattern. Extended configSchema and Config.Schema in config.ts with optional `github` object containing `profile` field. Created `ody auth github` command (cmd/auth/github.ts) with --profile flag, masked password prompt, cancellation handling, and credential persistence. Registered github subcommand in auth/index.ts. Updated `ody auth list` to display GitHub credential profiles with masked tokens and active profile indication. All validation commands pass (lint, fmt, typecheck).
2026-02-18: Added GitHub issue import to `ody task import` command — created packages/cli/src/lib/github.ts with GitHub namespace exporting parseInput (handles full URLs and owner/repo#number shorthand), fetchIssue (GET /repos/{owner}/{repo}/issues/{number} with optional Bearer auth, comment fetching, HTTP error handling for 401/403/404), and formatAsDescription (structured multi-line text output). Extended importPrompt.ts with GITHUB_IMPORT_PROMPT template and buildGitHubImportPrompt function. Refactored import.ts to support --github/-gh flag alongside --jira (mutually exclusive), extracted shared agent spawning into spawnAgent helper, added GitHub import flow (parse input, resolve profile, load auth, fetch issue, format, build prompt). Supports public repos without auth, --dry-run, and --verbose. All validation commands pass (lint, fmt, typecheck).
2026-02-19: Hardened `ody run` completion handling — replaced naive completion marker matching with strict standalone-line detection, removed premature process termination, now fails on non-zero backend exit codes, validates single-task completion status, and verifies loop-mode task states before honoring all-complete marker. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Enforced lockfile-strict installs in CI/release paths by updating Bun install steps to `bun install --frozen-lockfile` in .github/actions/setup-bun/action.yml, .github/workflows/prepare-release.yml, and .github/workflows/release.yml. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Pinned third-party GitHub Actions to immutable commit SHAs in CI/release workflows and shared setup action (`checkout`, `setup-bun`, `cache`, `create-pull-request`, `upload/download-artifact`, `action-gh-release`) with original major-version comments preserved. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Pinned all checkout steps in .github/workflows/release.yml (tag/build/release jobs) to `${{ github.event.pull_request.merge_commit_sha }}` so tagging, build artifacts, and release publishing always use the merged PR commit instead of moving `main`. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Added shared HTTP timeout/retry handling for remote update/import requests by introducing packages/cli/src/lib/http.ts (AbortController timeouts, bounded exponential backoff + jitter, transient retry rules for network/408/429/5xx) and migrating Installation.checkLatest, GitHub.fetchIssue (+ comments), and Jira.fetchTicket to use it. Added unit tests in packages/cli/src/lib/__tests__/http.test.ts for retry rules and timeout exhaustion. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Hardened release tag creation workflow by checking remote tag existence with `git ls-remote --tags origin "refs/tags/$TAG"` and always emitting `tag` output for downstream jobs, preserving idempotent reruns. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Optimized stream processing for large agent output by refactoring Stream.toOutput to incremental chunk handling with optional capture mode, updating run/plan/task import marker detection to chunk-based bounded matching, and aligning task edit stream calls. Added/updated stream tests for incremental callbacks, stop behavior, capture mode, and UTF-8 boundary safety. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Enforced main-branch context in prepare release workflow by setting checkout ref to `main` and create-pull-request base to `main`, preventing non-main dispatch drift. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Added workflow-level concurrency controls to CI, Release, and Prepare Release workflows using ref/event-specific grouping with cancel-in-progress enabled to prevent overlapping runs and release collisions. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Reused the shared `.github/actions/setup-bun` composite action in prepare-release and release build workflows, removing duplicated Bun setup/install steps while preserving frozen-lockfile installs and cache behavior. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Replaced sync task glob scans with shared async discovery (`glob.scan`) in task utilities and task list/edit/compact commands, added deterministic ordering + missing-directory handling, and added util tests for async task file discovery. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Parallelized task file loading with bounded concurrency across task utilities and task list/edit commands, preserving output order and warning behavior; added util tests for ordered/bounded async mapping. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Cached command-scope config/path derivations by reusing resolved tasks directory and loaded config values across run/plan/task import flows, updated run prompt construction to use a single config snapshot, and threaded optional tasksDir context through task utilities to avoid repeated Config.get/path joins. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Added per-command model overrides via new `models.run`/`models.plan` config fields with fallback to root `model`, wired run/plan to resolve and pass command-specific models, updated backend command builder signature, and added config parsing/resolution tests. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-19: Updated `ody task edit` to only show pending tasks by parsing frontmatter status during option building, filtering non-pending entries, and adding a graceful "No pending tasks to edit." early exit. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-20: Added docs for `ody auth` and subcommands (`github`, `jira`, `list`) with new auth docs section metadata, storage/permissions details, and examples; updated commands sidebar to include auth. All validation commands pass (bun lint, bun fmt, bun typecheck).
2026-02-20: Added `ody task import` command docs page with flags, input formats, behavior/authentication details, examples, and sidebar navigation update under task commands. All validation commands pass (bun lint, bun fmt, bun typecheck).
