{
  "name": "@ody/cli",
  "version": "0.0.1",
  "keywords": [
    "agent",
    "orchestration"
  ],
  "bin": "./dist/ody",
  "files": [
    "dist"
  ],
  "type": "module",
  "module": "./src/index.ts",
  "scripts": {
    "build": "bun build --production --compile --outfile=./dist/ody ./src/index.ts",
    "gen:schema": "bun run ./scripts/schema.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.1",
    "citty": "^0.2.1",
    "consola": "^3.4.2",
    "zod": "^4.3.6"
  }
}
