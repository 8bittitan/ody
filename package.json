{
  "name": "ody",
  "bin": "./dist/ody",
  "type": "module",
  "module": "./src/index.ts",
  "scripts": {
    "build": "bun build --production --compile --outfile=./dist/ody ./src/index.ts",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "fmt": "oxfmt --write .",
    "typecheck": "tsc --noEmit"
  },
  "files": ["dist"],
  "keywords": [
    "agent",
    "orchestration"
  ],
  "dependencies": {
    "@clack/prompts": "^1.0.0",
    "citty": "^0.2.0",
    "consola": "^3.4.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
