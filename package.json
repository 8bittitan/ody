{
  "name": "ody",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "fmt": "oxfmt --write .",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "oxfmt": "^0.31.0",
    "oxlint": "^1.46.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
