{
  "name": "ody",
  "private": true,
  "workspaces": [
    "packages/*",
    "internal/*"
  ],
  "scripts": {
    "build": "bun run --if-present --workspaces --parallel build",
    "build:cli": "bun run --filter '@ody/cli' build",
    "build:docs": "bun sync:schema && bun run --filter '@ody/docs' build",
    "dev:docs": "bun run --filter '@ody/docs' dev",
    "dev:desktop": "bun run --no-exit-on-error --filter '@ody/desktop' start",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "fmt": "oxfmt --write .",
    "test": "bun run --if-present --workspaces --parallel test",
    "typecheck": "bun run --if-present --workspaces --parallel typecheck",
    "sync:schema": "bun run --filter '@ody/cli' gen:schema && cp ./packages/cli/dist/configuration_schema.json ./packages/docs/public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@types/bun": "^1.3.10",
    "oxfmt": "^0.40.0",
    "oxlint": "^1.55.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
